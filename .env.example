VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — used by /api/delete-account. NEVER prefix with VITE_ or it
# would be bundled into client code. Get it from Supabase Project Settings
# > API > service_role secret. Also set this in the Vercel dashboard.
SUPABASE_SERVICE_ROLE_KEY=
